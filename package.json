{
  "name": "lambda-widgets-docs",
  "version": "0.0.0",
  "private": true,
  "description": "The documentation site for LambdaWidgets. The products it documents are built with dotnet; this package is only VitePress.",
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "1.6.4"
  }
}
