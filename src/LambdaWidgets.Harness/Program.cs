namespace LambdaWidgets.Harness;

/// <summary>
/// Item 3 replaces this with the Hardened Kestrel application: the dashboard page, the interpreter,
/// the server-side invoke proxy and the inspector. The entry point is here now so the project's
/// package references are restored and built by CI from the first commit.
/// </summary>
public static class Program {
    public static int Main(string[] args) {
        Console.Error.WriteLine("lambda-widgets: not implemented yet. See docs/status.md.");
        return 1;
    }
}
