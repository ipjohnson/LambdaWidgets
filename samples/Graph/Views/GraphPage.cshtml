@inherits Graph.GraphAppChartTemplates<Graph.GraphPage>
@if (Model.Detail is not null) {
  @Widget.Link("← Back", Links.Pages.Index())
}
@Draw(Model.Open)
@Draw(Model.Requests)
@Draw(Model.ByFunction)
